import { CanvasInputDirective } from './canvas-input.directive';

describe('CanvasInputDirective', () => {
  it('should create an instance', () => {
    const directive = new CanvasInputDirective();
    expect(directive).toBeTruthy();
  });
});
